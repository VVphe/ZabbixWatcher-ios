# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'

target 'MyZabbixWatcher' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyZabbixWatcher
  pod 'SwiftForms'
  pod 'SwiftyJSON'
  pod 'Alamofire'
  pod 'Toaster'

  target 'MyZabbixWatcherTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MyZabbixWatcherUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
